substitutions:
  imagemodel: "Gwen"  # Default fallback, can be overridden in main config
  imageheight: "720"  # Default fallback, can be overridden in main config

image:
  - file: https://raw.githubusercontent.com/RealDeco/xiaozhi-esphome/main/images/${imagemodel}/360x360/playing.png
    id: fallback_playing_image1
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE

  - file: https://raw.githubusercontent.com/RealDeco/xiaozhi-esphome/main/images/${imagemodel}/360x360/idle.png
    id: va_waiting_image1
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE

  - file: https://raw.githubusercontent.com/RealDeco/xiaozhi-esphome/main/images/${imagemodel}/360x360/listening.png
    id: va_listening_image1
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE

  - file: https://raw.githubusercontent.com/RealDeco/xiaozhi-esphome/main/images/${imagemodel}/360x360/thinking.png
    id: va_thinking_image1
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE

  - file: https://raw.githubusercontent.com/RealDeco/xiaozhi-esphome/main/images/${imagemodel}/360x360/replying.png
    id: va_replying_image1
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE

### other
  - file: https://raw.githubusercontent.com/RealDeco/sendspin-guition/main/images/error.png
    id: va_error_image
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE

  - file: https://raw.githubusercontent.com/RealDeco/sendspin-guition/main/images/timer_finished.png
    id: va_timer_finished_image
    resize: ${imageheight}x${imageheight}
    type: RGB565
    dither: NONE


### sendspin album art

generic_image:
  - platform: sendspin
    id: sendspin_album_art
    format: jpeg
    type: RGB565
    resize: ${imageheight}x${imageheight}
    source: ALBUM
    on_image_display:
      - script.stop: album_art_timeout_fallback
      - lambda: |-
          id(sendspin_album_art_valid) = true;
      - if:
          condition:
            lambda: 'return !id(album_art_slide_active);'
          then:
            - script.execute: album_art_apply_decoded
    on_image_error:
      - lambda: |-
          id(sendspin_album_art_valid) = false;
          if (!id(album_art_slide_active)) {
            lv_obj_add_flag(id(album_art_transition_widget), LV_OBJ_FLAG_HIDDEN);
          }
      - lvgl.image.update:
          id: album_art_widget
          src: fallback_playing_image1
      - lvgl.image.update:
          id: controls_album_art_widget
          src: fallback_playing_image1
      